"""Baby Whale — the course.

A learning layer *on top of* ``baby_whale_v4``. It never copies library code; it
narrates and imports it, so the material can't drift from what the tests verify.

- ``course.presets`` — progressive-motivation configs (gpt-minimal -> full).
- ``course.labs`` — autograder harness for the fill-in-the-blank labs.

See ``course/README.md`` for the syllabus.
"""
