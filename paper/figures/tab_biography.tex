% Table: the shipped checkpoint lineage, measured stage by stage.
\begin{table}[t]
\centering
\small
\begin{tabular}{@{}lll@{}}
\toprule
\textbf{Stage} & \textbf{Training signal} & \textbf{Capability probe} \\
\midrule
pre-train & CE $5.70{\to}4.39$ & bpb 3.15 / code 2.56 \\
mid-train & CE $3.09{\to}2.32$ & bpb 1.71 / code \textbf{1.15} \\
SFT       & CE $1.61{\to}0.12$ & answers in-format \\
DPO       & \emph{diverged} ($0.15{\to}3.50$) & --- \\
GRPO      & reward $2.25{\to}16.0$ (max) & pass@1 $=0$ \\
\bottomrule
\end{tabular}
\caption{\textbf{A staged lineage, measured}: one $\sim$11M-parameter model
chained through the lifecycle by the CLI
(\texttt{stage\_evals.json}; full matrix in Appendix~\ref{app:measure}).
Bits-per-byte on the pre-training corpus~/ on repository code unseen by the
pre-train checkpoint (baseline 8.0). The last rows are candid: DPO's small
run diverged, and GRPO saturates its partial-credit reward while pass@1 stays
zero---the likelihood-vs-capability gap the evaluation module teaches.}
\label{tab:biography}
\end{table}
