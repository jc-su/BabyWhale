% Table: taxonomy of the 8 inaccuracies found by the expert line-audit.
\begin{table}[t]
\centering
\small
\begin{tabular}{@{}p{2.35cm}cp{3.6cm}@{}}
\toprule
\textbf{Error class} & \textbf{\#} & \textbf{Example (corrected)} \\
\midrule
Wrong functional form & 2 & router gates with $\sqrt{\mathrm{softplus}}$, not softmax; balance bias moves by a fixed step, not $\propto$ load \\
Wrong mechanism scope & 3 & HCA/CSA keep a \emph{raw local window} + compressed distant keys; \texttt{produce} also mixes streams \\
Wrong index/offset & 1 & MTP head $k$ predicts $t{+}k{+}2$, not $t{+}k$ \\
Wrong factorization & 1 & MLA up-projects with \emph{one} matrix, not per-head pairs \\
Wrong attribution & 1 & std-normalized advantage is GRPO's, not RLOO's \\
\bottomrule
\end{tabular}
\caption{\textbf{What drifts when documentation is written from memory:} the 8
inaccuracies our line-audit found in early drafts of the theory-to-code
bridges (all corrected). None were catchable by symbol-level checks alone,
which is why the course \emph{pastes} the real code (semantic claims sit next
to their evidence) and grades labs by functional equality with the
implementation.}
\label{tab:audit}
\end{table}
